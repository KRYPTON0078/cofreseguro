id: rule_129
label: otp_forward
weight: 0.34
enabled: true
locales:
- en
pattern: (?:\b(forward|send)\s{1,3}(this\s{1,3})?(otp|code)\s{1,3}(to|via)\b)|\b(limited\s{1,3}time)\b
description: Unique otp_forward detector variant 129 (en).
