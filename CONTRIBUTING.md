# Contributing

1. Use conventional commits (`feat:`, `fix:`, `docs:`, `test:`, `ci:`).
2. Add tests for analyze pipeline changes.
3. Keep Ollama optional — demos must work with rules+ML fallback.
4. Support English and Portuguese strings where user-facing.
